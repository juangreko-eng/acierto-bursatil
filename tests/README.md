# tests

Pruebas unitarias para `src/variables`, `src/modelos` y, sobre todo, `src/backtesting` — el motor walk-forward es el componente donde un error (fuga de información futura, mal manejo de fechas) invalida silenciosamente todos los resultados.
